<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.UserArticleMapper">
    <!--查询目前最大的会议号-->
    <select id="selectMaxArticleId" resultType="Integer">
      select IFNULL(max(article_id),0)  from `user_article`
    </select>

    <select id="selectArticleInfo" parameterType="Integer" resultType="com.example.pojo.UserArticle">
        select t1.*,
        t3.meeting_id,t3.theme,t3.name,t3.abbreviation,t3.starttime,t3.endtime,t3.location,t3.ref,t3.detail,
        if(endtime &lt; now(),0,1) as conference_status
        from user_article t1 inner join meeting_article t2 on t1.article_id = t2.article_id
        inner join meetinginfo t3 on t3.meeting_id = t2.meeting_id
        where t1.user_id = #{userId}
    </select>

    <select id="selectArticleInfo2" resultType="com.example.pojo.UserArticle">
        select t1.*,
        t3.meeting_id,t3.theme,t3.name,t3.abbreviation,t3.starttime,t3.endtime,t3.location,t3.ref,t3.detail,
        if(endtime &lt; now(),0,1) as conference_status
        from user_article t1 inner join meeting_article t2 on t1.article_id = t2.article_id
        inner join meetinginfo t3 on t3.meeting_id = t2.meeting_id
        <where>
            <if test="userId != null">
                t1.user_id = #{userId} and
            </if>
            <if test="articleName != null">
                t1.title LIKE "%"#{articleName}"%"
            </if>
        </where>
    </select>
</mapper>

