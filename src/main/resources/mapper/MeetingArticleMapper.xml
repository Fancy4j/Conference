<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.MeetingArticleMapper">

    <select id="getArticleIds" parameterType="Integer" resultType="java.lang.Integer">
        SELECT DISTINCT * FROM (select t1.article_id from meeting_article t1
        inner join user_article t2 on t1.article_id = t2.article_id
        where meeting_id = #{meetingId} and t2.status != 10
        ) tt
    </select>
</mapper>

